```
example_project/
│
├── app.py               # Flask 애플리케이션 코드
├── templates/           # HTML 템플릿 디렉토리
│   └── index.html       # HTML 파일
└── static/              # CSS, JS, 이미지 파일 등 정적 파일을 관리할 디렉토리
    └── style.css        # CSS 파일 (선택 사항)
```